# ===========================================
# BookaRide - Environment Variables Template
# ===========================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git - it contains secrets!

# --- Google Workspace Gmail API (REQUIRED for booking confirmations) ---
# Use ONE of these two options:

# Option A: Path to your JSON credentials file (for local/server with file access)
GOOGLE_SERVICE_ACCOUNT_FILE=backend/bookaride-gmail-credentials.json

# Option B: Full JSON as single line (for Render, Heroku, etc - no file upload)
# Paste your entire JSON between the quotes, as one line:
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project",...}

# The email address to send FROM (must exist in your Google Workspace)
SENDER_EMAIL=noreply@bookaride.co.nz

# --- Other common variables (may already be set) ---
# MONGODB_URI=mongodb+srv://...
# TWILIO_ACCOUNT_SID=...
# TWILIO_AUTH_TOKEN=...
# TWILIO_PHONE_NUMBER=...
# STRIPE_SECRET_KEY=...
